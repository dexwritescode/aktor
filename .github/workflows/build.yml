name: Build

on:
  push:
    branches: [ "main" ]
    paths-ignore:
      - '**.md'
      - 'docs/**'
  pull_request:
    branches: [ "main" ]
    paths-ignore:
      - '**.md'
      - 'docs/**'

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Build (default)
        run: cargo build --locked

      - name: Build (serde)
        run: cargo build --locked --features serde

      - name: Build (http)
        run: cargo build --locked --features http

      - name: Build (serde + http)
        run: cargo build --locked --features serde,http

      - name: Clippy (default)
        run: cargo clippy --locked -- -D warnings

      - name: Clippy (serde + http)
        run: cargo clippy --locked --features serde,http -- -D warnings

      - name: Format check
        run: cargo fmt --check
